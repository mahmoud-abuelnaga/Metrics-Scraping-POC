apiVersion: v1
kind: Service
metadata:
  name: shoehub-app
  namespace: production
  labels:
    app.kubernetes.io/name: shoehub-app
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: shoehub-app
